Change exactly one function or one module per step. No cross-cutting refactors,
no renaming across files, no reorganising directories. If a task seems to need
more than one atomic change, stop and list the steps for approval first.
