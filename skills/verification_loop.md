After every edit, run the project's test suite yourself with a real command.
Never report a result you did not observe in actual output. If tests fail,
do not attempt a second fix before reporting the failure.
